# Building Block View

This section covers the main building blocks for the application.

## Key components

- `diff.rs` - Handles logic around working with diffs.
- `browser.rs` - Handles file browsing logic.
- `ui.rs` - Handles UI rendering.
- `app.rs` - Handles application state management.
- `main.rs` - Implements the CLI for the application.