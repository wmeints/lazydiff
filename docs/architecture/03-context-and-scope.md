# Context and Scope

## Business Context

The application is mainly meant as a simple diff viewer that allows you to 
quickly share a patch for use in documentation or to share changes with a 
colleague. The tool has no interactions with other tools.

## Technical Context

The tool is meant to be used through any modern terminal. We expect users to
use the tool in SSH sessions as well as on their desktop PC.
